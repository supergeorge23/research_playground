# Source Code

Reusable research code belongs here.

Suggested layout after implementation starts:

- `src/data/`
- `src/models/`
- `src/training/`
- `src/evaluation/`
- `src/scaling_laws/`
- `src/infra/`

Experiment-specific glue should stay in `experiments/`; reusable components
should move into `src/`.
