# Configs

Store versioned configs here. Prefer small, explicit YAML files over hidden
defaults in scripts.

Suggested layout:

- `configs/data/`
- `configs/model/`
- `configs/training/`
- `configs/infra/`
- `configs/sweeps/`
