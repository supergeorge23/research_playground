# Artifacts

This folder stores lightweight manifests and small reviewable outputs.

Do not commit raw datasets, checkpoints, large logs, tensorboard directories, or
wandb runs. Put those in external storage and reference them from
`artifacts/manifests/`.
